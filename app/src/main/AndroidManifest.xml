<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.droidcraft">
    <!-- Permissions INTERNET and ACCESS_NETWORK_STATE are not needed for a local piano app and have been removed for robustness. -->
    <application
        android:allowBackup="true"
        android:label="DroidCraft App"
        android:theme="@style/Theme.DroidCraft"> <!-- Updated to a custom Material3-compatible theme -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>